# AI晨间规划（DeepSeek）

在原“明日规划”页面标题下新增“AI帮我安排”按钮。输入今天的计划，生成预览后确认填入。现有四象限、时间表、统计与保存按钮保持原逻辑。

## 配置

项目已有Vercel API目录。部署环境使用Node.js 20或更新版本，设置服务器环境变量：

- `DEEPSEEK_API_KEY`：DeepSeek密钥，必填，禁止写入前端。
- `DEEPSEEK_MODEL`：可选，默认`deepseek-chat`，可按账户可用模型调整。

配置后重新部署。直接双击HTML或只提供静态文件的服务器不能运行`/api/ai-plan`。本地可通过Vercel开发服务器运行；密钥放在不提交的`.env.local`。本次没有自动部署或更改已有环境变量。

如果使用本项目已有的 Ubuntu/Nginx/PM2 脚本：

```bash
cd /var/www/deepmind
export DEEPSEEK_API_KEY='你的密钥'
sudo -E bash deploy-to-server.sh
```

脚本会启动 `ai-plan-server`（127.0.0.1:3100），并把 `/api/ai-plan` 反向代理到它；静态页面与原有邮件服务继续按原配置运行。生产环境建议将变量写入受保护的 systemd/PM2 环境配置，而不是写进仓库文件。部署后可检查：

```bash
curl http://127.0.0.1:3100/api/ai-plan-health
pm2 logs ai-plan-server --lines 50
```

接口依据：https://api-docs.deepseek.com/guides/json_mode/

## 行为

- 发送输入文字、当前时间表、已有标签给DeepSeek，不发送历史复盘。
- 半小时段内最多30分钟；长任务拆分到多个时间段。未排入任务和估算需要在预览中确认。
- 四象限任务追加，同象限同名任务保留原内容；未涉及的时间段不改动。
- 时间段已有非空内容时逐项显示替换清单，必须勾选确认。
- 原表单在生成后变化则禁止填入，要求重新生成。
- 不自动提交规划，不自动写入复盘，不调用完成率更新。使用原“保存规划”后仍会执行原网站既有的复盘同步逻辑。
- 标签沿用已有标签的颜色提示；分类统计继续使用原网站的任务名称匹配规则，没有改动统计口径。
- API错误、超时或无效结构不会修改原表单。无密钥明确显示未配置，不用假数据冒充AI。

## 验收

1. 输入“制作视频90分钟，11点直播3小时”，核对总时长和固定时间。
2. 验证午休等预填安排的冲突清单，未勾选不能填入。
3. 验证原四象限任务、未涉及时间段保留，填入后统计图更新。
4. 输入没有说明时长的任务，核对估算提示；时间不足时核对未排入原因。
5. 修改输入重新生成，或在预览后修改原表单，验证旧预览不能误覆盖。
6. 配置真实密钥后验证服务；离线、密钥错误、额度不足时验证错误提示。

本次已执行结构校验和模拟接口测试；真实模型调用需要密钥。浏览器自动化受本机启动权限限制，尚未完成实际页面验收。公开开放前，应按部署平台配置调用限额/访问控制；原有前端会员状态不能充当后端鉴权。
